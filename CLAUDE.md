# Investment Notes - Project Guidelines

## 分析・執筆ワークフロー

- **分析はまずこの会話の中で行い、有意義と判断したものだけをwebにまとめる**
- 流れ：①会話内でデータ取得・分析・議論 → ②採用する内容を合意 → ③.qmdファイルに反映
- Claudeが勝手に分析結果をqmdに書き込まない。必ず会話内での確認を経てから反映する

## 投資学習・PDCAワークフロー

- **投資判断・失敗・教訓は Obsidian Vault を一次記憶として蓄積する**
- この投資分析プロジェクト内でのAIの呼称は **しぐまAI**。分析メモ、Daily、会話内でAIの暫定見解を出すときは、必要に応じて **「しぐまAIの考え」** という見出しを使う
- Obsidian Vault: `/mnt/c/Users/BlueF/Dropbox/obsidian1/10_Specialty/投資分析 with Claude/`
- `investment-notes/learning/` は清書・公開用の二次置き場であり、日々のPDCA本体ではない
- 日々の判断・心理・売買ログは Obsidian の `会話ログ/` に日付付きで保存する
- 一般化できる教訓は Obsidian の `テーマ/投資ルール.md` に統合する
- テーマ知識は Obsidian の `テーマ/`、個別銘柄知識は Obsidian の `銘柄/` に統合する
- **知識とノウハウは分ける。** 知識は「何を理解するか」で、テーマ/銘柄ノートに置く。ノウハウは「どう判断・行動するか」で、`テーマ/投資ルール.md` と `テーマ/しぐまAI_運用ガイド.md` に置く
- 長文資料や講座を処理するときは、まず知識として整理し、再利用できる行動ルールだけをノウハウへ抽出する。`投資ルール.md` に理論説明を長く書かない
- 大きな損益変動や心理的に重要な局面は、日付付きの会話ログとして残し、その後テーマ/銘柄ノートへ統合する
- 目的は予想を当てることではなく、仮説・サイズ・撤退条件・感情を記録し、次の判断精度を上げること

## セッション開始時の外部記憶ロード

新しいAIセッションや文脈が薄い状態で投資相談を受けたら、まず以下を確認する。

1. Obsidian Vault の `_INDEX.md`
2. Obsidian の `運用方針.md`
3. 関連する `テーマ/*.md`
4. 関連する `銘柄/*.md`
5. 必要に応じて直近の `会話ログ/*.md`

ユーザーが半導体・AIインフラ・個別銘柄の知識を覚えている前提にしない。AIがObsidianとこのリポジトリを外部記憶として参照し、足りない最新データだけ取得して判断する。

### 金融機械学習講座の利用

- 松尾研究室の金融機械学習講座資料は `/home/bluefelix/mystudy/Financial_ML/lecture` にある
- 講座内容をそのまま転載するのではなく、株取引としぐまAI運用に使える知識へ再構成する
- Obsidian の `テーマ/金融機械学習_実戦応用.md` と `テーマ/しぐまAI_運用ガイド.md` を、金融ML・ファクター・バックテスト・ラベリング・メタラベリング・Daily Watch設計の参照元として使う
- 特に Day3 ラベリング/メタラベリング、Day4 バックテスト/CPCV、Day6 CTA/ボラターゲット、Day7 ケリー基準・Optimal f・効用関数によるサイズ決定、Day8 帰納バイアス・日本株ファクター・LLMの推論/バイアス、Day9 低S/N比・非定常性・米国から日本株へのファクター伝播・ポストLLM投資運用は実戦に優先適用する
- しぐまAIはLLMを予言者として扱わない。Obsidian/CLAUDE.mdを明示的な帰納バイアスとして読み、事実/推論/仮説/反証条件/価格ライン/サイズ/期限に分けて出力する
- LLM活用では、会社名・テーマ語・有名人・AI/NVIDIA/半導体などの強いトークンによるバイアスを疑い、最新株価・決算日・イベント・ニュースは必ず最新データで確認する
- 重要な売買判断では、Bull/Bear/定量リスク/イベント/執行の役割に分けた「しぐまAI投資委員会」形式で、漏れ防止と反証条件の確認を行う

### 書籍・長文コンテンツのリサーチ方針

- 半導体戦争などの重要書籍、note、長文レポートは、ユーザーが全文を読む時間がない前提でAIが外部情報を取りに行く
- 著作権を理由に「絶対にアクセスしない」運用にはしない。公開されている書誌情報、目次、著者インタビュー、書評、関連解説、合法的に閲覧できる抜粋には積極的にアクセスする
- ただし文面の丸写しや本文の代替になる保存は避け、内容を投資判断に使える知識へ再構成する
- 保存時は単なる要約ではなく、テーマ仮説、サプライチェーン、銘柄への示唆、反証条件、監視指標に変換してObsidianへ統合する

## 分析ノートブック（.ipynb）の方針

- **探索的な定量分析は `analysis/` フォルダの `.ipynb` に保存する**
- 命名規則：テーマ単位（例：`analysis/copper.ipynb`、`analysis/photonics.ipynb`）
- ユーザーが自分でVS Code等から直接実行できる形にしておく
- カーネルは `investment-notes`（`.venv`を登録済み）を使用する
- **webサイト（.qmd）には煮詰めた結論・チャートのみを載せる。分析の全過程は.ipynbに残す**

## 分析の順序と目的

**分析は必ず以下の順序で行う：**

1. **未来予測（定性・マクロ）を先に行う** — 技術トレンド・市場構造・ボトルネックの変化を考察し、「どういう未来が来るか」の仮説を立てる
2. **その仮説に対して個別企業を評価する** — 「その未来が来たとき、この企業は割安か」という問いで定量指標を使う

現在の株価・時価総額の絶対値から分析を始めない。日々の価格変動に左右されず、構造的な本質を見出すためのモデル。

## 定量分析の表示ルール

- **日本株の財務指標（時価総額・売上等）は必ずJPY→USDに変換して表示する**（yfinanceでUSDJPY=Xレートを取得して換算）
- **時価総額等の絶対値はレンジ表示にする**（市場価格は常に変動するため、正確な値より大まかなレンジが実用的）
  - 例：`$18.3B` ではなく `~$18B` や `$15〜20B` のように表示

## 投資対象ユニバース

- **対象市場：米国上場株（NYSE・NASDAQ）および日本株（東証）のみ**
- 理由：実際に購入できる市場に限定することで、分析を実用的に保つ
- LSE（ロンドン）、ASX（オーストラリア）、EUR市場（ユーロネクスト等）上場銘柄は対象外
  - 例：Glencore（LSE）、Lynas Rare Earths（ASX）、Air Liquide（EPA）は分析対象外
- ADR（米国預託証券）として米国市場に上場している銘柄は対象に含む（例：BHP）

## 投資戦略・資金配分

- **総資金：50万円**
- **リスク資金（10万円）：** 全損OKの高リスク・高リターン枠。小型・投機的銘柄も対象
- **運用資金（40万円）：** 通常運用枠。モートが明確な質の高い銘柄中心
- **目標リターン：2028年までに最低3倍（¥150万円）**
  - 根拠：AI市場規模は2028年までに2〜3倍、2030年までに5倍と予測
  - 上振れ銘柄を選定できれば市場平均を超えるリターンは十分に狙える
- 定量分析では、この2つのバケット（リスク枠・運用枠）への適合度も評価する

## 投資哲学

- **分析対象はその技術・市場でトップレベルの企業を優先する**
- 理由：評価が高い（割高に見える）こと自体が、市場がそのモート（競争優位の堀）を認識している証拠であり、AI特需の恩恵が最も集中するのはそういった企業であるべき
- 割高でも買うか・押し目を待つかの判断軸は「AI特需を織り込んだ成長率に対してバリュエーションが回収可能か」（PEG的視点）
- 定量分析では、単純なPERや絶対値ではなく、成長率・モートの強度・需給構造を合わせて評価する

### AI特需に関する前提（毎回説明不要）

- **AI特需は必ず実現するという確信のもとで投資している**
- NVIDIAですらここからあと4〜5倍になるポテンシャルがあると見ている
- AI特需が実現しなかった場合は資金ゼロになっても構わないという覚悟で臨んでいる
- この前提はすべての銘柄選定・バリュエーション評価の土台であり、毎回確認・説明は不要

## リサーチ方針

- MCP・APIを使うもの（financial-datasets等）以外のリサーチは、必ず外部AIが担当する
- ClaudeがWebサイトを独自に調査・検索することはNG（WebSearchツール等の使用禁止）
- 定性情報（企業概要・競合分析・ニュース等）は外部AIから提供されたものをベースに使用する

## データソース戦略

| 対象 | ツール | 備考 |
|---|---|---|
| 日本株の最新株価 | yfinance | `6857.T` のように `.T` を付ける。イントラデイ取得も可能 |
| 米国株の最新株価 | yfinance | `NVDA`, `MU`, `VRT` など |
| 日本株のJPX/J-Quants詳細データ | stock-dashboard / JPX CLI / ローカルDB | API契約状態に注意。現状、ローカルDBは2026-06-10までが実用ライン |
| 米国主要銘柄（大型株） | MCP (financial-datasets) | S&P500・NASDAQ100構成銘柄など。フリー枠で取得可能 |
| 米国ニッチ株（小型・中型） | yfinance | POET・RKLBなど |

## Python環境

- **パッケージ管理**: `uv`（`pyproject.toml` + `uv.lock`でバージョン固定）
- **仮想環境**: `investment-notes/.venv`（gitignore済み）
- **環境再現**: 別PCでclone後に `uv sync` するだけで同じ環境が作られる
- **stock-dashboardの`.venv`は使わない**（このプロジェクトは独立した環境）

### パッケージ追加方法
```bash
cd ~/my-apps/investment-notes
uv add <package-name>
```

## Quartoコード実行

- `_quarto.yml`に`execute: freeze: auto`を設定済み
- コードチャンクはローカルで実行し、結果を`_freeze/`にキャッシュしてgit管理
- GitHub Actions上ではPythonを実行しない（キャッシュを使う）
- ローカルでレンダリング: `quarto render`

## サイト構成方針

- トップページ・Macroセクション：マクロトレンドを前面に出す
- 個別銘柄（stocks/）：深い階層に配置、メインには出さない
- 定性分析：Markdownで記述（.qmdファイル）
- 定量分析：Pythonコードチャンクで記述（同じ.qmdに埋め込み）

## Obsidianノート管理

- **Vault：** `/mnt/c/Users/BlueF/Dropbox/obsidian1/10_Specialty/投資分析 with Claude/`
- **構成：**
  - `_INDEX.md` — 銘柄・テーマへのリンク集（ハブ）
  - `テーマ/` — 投資テーマごとの分析ノート
  - `銘柄/` — 銘柄ごとの分析ノート
  - `会話ログ/` — 整理前の生の会話ダンプ（日付_トピック.md）
- **運用ルール：**
  - `テーマ/`または`銘柄/`に新しいノートを作成したら、必ず`_INDEX.md`の該当セクションにリンクを追加する
  - ノート内では`[[銘柄名]]`や`[[テーマ名]]`でリンクを張り、Graph Viewで関係性が見えるようにする
  - Graph ViewはObsidianアプリのUI機能のため、Claudeからは操作不可。リンクを正しく張ることで自動反映される
  - 知識とノウハウを分ける。テーマ/銘柄ノートには知識・仮説・反証条件・監視指標を置き、投資ルール/しぐまAI運用ガイドには売買前チェック・禁止事項・判断順序・依頼テンプレを置く
  - **知識の更新は弁証法的に行う：** 同じテーマに関する新しいリサーチが来たら、`会話ログ/`にその回の議論をダンプしつつ、`テーマ/`・`銘柄/`ノートは現時点での統合理解に上書き更新する。過去の経緯は会話ログに残すため、テーマ/銘柄ノートは常に「今の最善理解」だけを反映した状態に保つ

## セッション間の記憶管理

- **会話ログはObsidianの `会話ログ/` に保存する**（Claudeのmemoryシステムは使わない）
- セッション終了時または区切りのよいタイミングで、`会話ログ/日付_トピック.md` としてダンプする
- 命名例：`2026-05-19_AIデータセンターPhase2電力分析.md`
- 次セッション開始時にユーザーがそのファイルを貼り付けることで文脈を引き継ぐ

## デプロイフロー

1. ローカルで分析・執筆
2. `quarto render`でビルド（コードチャンクが実行され`_freeze/`に保存）
3. `git add . && git commit && git push origin main`
4. GitHub Actionsが`gh-pages`ブランチを更新
5. GitHub Pagesに自動反映（数分）
